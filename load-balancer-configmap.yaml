apiVersion: v1
data:
  nginx.conf: |
    events {

    }

    http {
      server {
        listen 80;

        location / {
          proxy_pass http://dolibarr:80;
        }
      }
    }
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: nginx-config
